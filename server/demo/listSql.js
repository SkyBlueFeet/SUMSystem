var list = {
    // 查询所有用户
    queryAll: 'SELECT * FROM tableData',
    queryLimit: 'SELECT * FROM tableData limit ?,?',
    queryLength: 'SELECT COUNT(*) length from hl_creater_msg'
}

module.exports = list